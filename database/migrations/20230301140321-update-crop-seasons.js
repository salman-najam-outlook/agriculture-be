'use strict';

module.exports = {
  async up (queryInterface, Sequelize) {
    /**
     * Add altering commands here.
     *
     * Example:
     * await queryInterface.createTable('users', { id: Sequelize.INTEGER });
     */
    await queryInterface.sequelize.query("UPDATE options SET name = 'wet' WHERE name LIKE 'rainy/wet season';");
    await queryInterface.sequelize.query("UPDATE options SET name = 'dry' WHERE name LIKE 'short rains';");
  },

  async down (queryInterface, Sequelize) {
    /**
     * Add reverting commands here.
     *
     * Example:
     * await queryInterface.dropTable('users');
     */
  }
};
